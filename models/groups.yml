# groups: name + owner (name, email only). Custom contact (slack/github) not supported in schema.
groups:
  - name: finance
    owner:
      name: Firstname, Lastname
      email: firstname.lastname@example.com
  - name: product
    owner:
      email: product@jaffleshop.com