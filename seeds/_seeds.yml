version: 2

seeds:
  - name: employees
    columns:
      - name: employee_id
      - name: email
      - name: customer_id
        tests:
          - not_null
          - unique  
    config:
      # grants:
      #   select:
      #     - roles/bigquery.dataViewer 
      #     - roles/bigquery.jobUser    